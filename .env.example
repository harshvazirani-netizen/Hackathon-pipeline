# Copy to .env and fill in. .env is gitignored.
FAL_KEY=                 # https://fal.ai/dashboard/keys  (generation)
ANTHROPIC_API_KEY=       # https://console.anthropic.com  (script direction + vision QA)
ELEVENLABS_API_KEY=      # https://elevenlabs.io          (voiceover)
SHOTSTACK_API_KEY=       # https://shotstack.io           (assembly)
SHOTSTACK_ENV=stage      # 'stage' = free sandbox, 'v1' = production
